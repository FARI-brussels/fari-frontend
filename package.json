{
  "name": "fari",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:packages": "pnpm -r -F '@fari/*' run build",
    "dev": "concurrently --kill-others \"pnpm --filter @fari/components dev\" \"pnpm --filter @fari/playground dev\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^22.13.11"
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
