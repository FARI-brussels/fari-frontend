{
  "name": "fari-frontend",
  "version": "1.0.2",
  "main": "index.js",
  "scripts": {
    "build:packages": "pnpm -r -F '@fari-frontend/*' run build",
    "dev": "concurrently --kill-others \"pnpm --filter @fari-frontend/components dev\" \"pnpm --filter @fari-frontend/playground dev\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^22.13.11"
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
